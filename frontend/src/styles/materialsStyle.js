import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  downloadBtn: {
    width: '100%',
    height: '50px',
    backgroundColor: '#1e90ff',
    borderRadius: '3px',
    border: '0px',
    color: '#fff !important',
    fontSize: '20px',
    fontWeight: '700',
  },
});
